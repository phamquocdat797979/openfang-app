// OpenFang — Frontend Config
// API key được lưu an toàn trên server, không có ở đây
const OPENFANG_CONFIG = {
  GEMINI_MODEL: 'gemini-2.5-flash', // default model
  API_PROXY_URL: '/api/gemini',      // goes through our backend server
};
